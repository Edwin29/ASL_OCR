"""Add source-grounded explanations without replacing approved Korean prose."""
from pathlib import Path
root=Path(__file__).resolve().parents[3]
target=root/'docs/ASL_OCR_TECHNICAL_OVERVIEW.md'
s=(root/'docs/technical-overview/TERMINOLOGY_ADDED.md.snapshot').read_text(encoding='utf-8-sig')

def before_source(section, prose):
    global s
    start=s.index('### '+section+' ')
    end=s.index('구현:',start)
    s=s[:end]+prose.strip()+'\n\n'+s[end:]

s=s.replace('## 4. 결과를 결정하는 핵심 처리\n','''## 4. 결과를 결정하는 핵심 처리

이 프로젝트의 핵심은 인식 모델 하나가 모든 일을 마치는 데 있지 않다. 촬영 판단, 문서 구조, 수식 출력, 저장과 재시도, 현재 상태의 출력을 나누고 연결한 구성이다. 아래는 현재 구현으로 확인할 수 있는 해결 방식과 효과다. 다른 시스템보다 정확하거나 빠르다는 비교 실험 결과를 뜻하지 않는다.

| 다루는 어려움 | 사용한 방식 | 얻는 효과와 남는 한계 |
|---|---|---|
| 인식 흔들림과 페이지 넘김을 구분 | 여러 영상의 좌우 번호 쌍과 추가 변화 조건 결합 | 한 번의 불일치를 새 촬영으로 넘기지 않음. 반복된 오인식이나 장기 대기는 가능 |
| 사진의 영역 목록을 실제 읽기 조작으로 연결 | 페이지 중간 표현을 만든 뒤 읽기 항목으로 변환 | 원문·위치·수식·표의 관계와 탐색을 연결. 인식 누락을 자동 복원하지 않음 |
| 수식의 관계를 두 출력에 전달 | 수식 구조 트리에서 음성과 점자를 각각 생성 | 분자·분모 같은 관계를 공통으로 사용. 원문의 수학적 정확성 증명은 아님 |
| 통신 실패·재시작 후 중복 효과 방지 | 전송 보관, 요청 식별자, 처리 기록, 불변 게시 버전 | 재시도와 위치 복구의 기준을 유지. 모든 물리 동작을 한 번만 수행한다고 보장하지 않음 |
| 연속 입력 중 오래된 출력 방지 | 현재 읽기 번호와 단말 재생 권한을 따로 관리 | 늦은 작업 결과를 구분. 실제 스피커·핀 상태는 별도 확인 |

다음 절은 이 방식이 어느 입력을 받아 어떤 판단으로 다음 단계에 넘기는지 설명한다. 기능적 효과는 현재 코드와 시험에 근거하며, 처음 채택할 당시의 모든 설계 의도나 대안 비교를 확인한 것은 아니다.
''',1)

before_source('4.2','''#### 한 번의 인식 차이를 새 페이지로 보지 않는 이유

같은 책장을 두고도 하단의 `28`을 `2B`처럼 다르게 읽을 수 있다. 그래서 ‘이전과 다르게 읽혔다’와 ‘새 페이지라는 근거가 모였다’를 나눈다. 이전 쌍이 다시 정확히 나오면 중복을 빨리 확인할 수 있지만, 새 쌍은 여러 영상에서 반복되어야 한다. 관측들이 제각각이면 기존 쌍과 모두 달라도 판단을 보류한다.

기준26/27에 대해 새28/29가 다섯 번 반복된 예제에서는 앞 네 번은 판단 불가, 다섯 번째에 다름과 번호 변화 근거가 나왔다. 반대로 서로 다른 오인식 쌍 다섯 개는 판단 불가였다. 이 차이는 단순히 관측 수를 채우는 것이 아니라 새 내용의 일관성을 확인한다는 뜻이다.

전송을 확인한 페이지를 기준으로 쓰는 것도 중요하다. 아직 보내지 못한 촬영을 완료 기준에 넣으면 이후 재촬영을 이미 전달한 중복으로 오해할 수 있다. 다만 기준 문자열 자체가 계속 잘못 읽히는 경우까지 없애는 장치는 아니다. 실제 영상 변화 조건과 다음 후보의 품질 판단도 통과해야 촬영이 진행된다.''')

before_source('4.3','''#### 좌우 이미지의 겹침은 별도의 문제다

중복 촬영 억제는 ‘이 펼친 면을 전에 보냈는가’를 판단한다. 반면 책등의 겹침은 ‘경계에 걸친 화소를 어느 쪽 이미지에 남길 것인가’의 문제다. 현재 보정 입력은 경계의 불확실한 부분을 양쪽에 남길 수 있다. 경계를 잘못 잡아 글자 일부를 잃는 대신 중복 화소를 보존하는 선택이다.

이 처리가 겹친 본문을 나중에 자동 삭제한다는 뜻은 아니다. 좁은 경계 띠의 보존, 같은 펼친 면 재촬영, 같은 파일 재전송은 각각 다른 원리로 다룬다. 최종 읽기 자료에서 내용이 반복되는 문제는 어느 경계에서 생겼는지 나누어 확인해야 한다.''')

before_source('4.4','''#### 인식 결과와 읽기 항목 사이에 중간 표현을 두는 이유

인식 모델의 결과에는 글자뿐 아니라 영역 종류·위치·순서가 있다. 이것을 곧바로 한 문장으로 합치면 표의 행·열이나 수식의 구조, 문제의 지문과 선택지 관계를 이후 조작에서 활용하기 어렵다. 현재 경로는 이 정보를 먼저 문서 데이터로 정리한 뒤 사용자가 선택할 단위로 바꾼다.

| 단계 | 책임 | 예: 지문·분수·선택지가 있는 페이지 |
|---|---|---|
| 인식 영역 | 사진에서 읽힌 내용·종류·위치·순서 제공 | 지문 문자열, 분수 표기, 선택지 문자열 |
| 페이지 중간 표현 | 종류별 구조와 원문, 문제 구성 관계 보관 | 분수의 AST, 문제에 속한 지문·수식·선택지 식별자 |
| 읽기 항목 | 방향키로 선택할 목록과 내부 탐색 구조 제공 | 지문→수식→선택지로 이동하며 수식에는 점자값 생성 |

문제에 묶인 항목도 독립적으로 선택할 수 있도록 다시 펼친다. 이때 같은 항목 식별자를 두 번 방문하지 않게 하여 문제 내부와 페이지 최상위에서 같은 항목을 반복하는 것을 막는다. 동일한 글자가 적힌 다른 항목을 전부 삭제하는 규칙은 아니다.

이 중간 표현은 인식 품질을 보증하는 장치가 아니다. 모델이 영역을 누락했거나 위치가 유효하지 않아 건너뛴 경우, 남은 데이터만으로도 형식이 올바를 수 있다. 구조를 다룰 수 있다는 것과 사진 내용을 빠짐없이 옮겼다는 것을 구분해야 한다.''')

before_source('4.5','''#### 문자열의 순서보다 관계를 전달한다

분수 표기에는 분자와 분모가 서로 다른 역할로 들어 있다. AST는 각 숫자를 분수 아래의 어느 자리에 놓았는지 기록한다. 출력기는 문자열에서 그 관계를 다시 추측하는 대신 같은 구조를 받아 자기 규칙으로 읽거나 점을 만든다. 예를 들어 `\\frac{1}{2}`는 문자열에서 분자가 먼저 나오지만 한국어 음성은 ‘2분의 1’이고, 현재 점자 규칙도 분모와 분수표 다음에 분자를 배치한다.

```mermaid
flowchart TD
    Formula["입력 수식: 분자 1과 분모 2"] --> Tree["공통 구조: 분수"]
    Tree --> Numerator["분자 자리의 숫자 1"]
    Tree --> Denominator["분모 자리의 숫자 2"]
    Tree --> Speech["음성 규칙: 2분의 1"]
    Tree --> Braille["점자 규칙: 분모와 분수표 다음에 분자"]
    Speech --> Words["합성할 한국어 문장"]
    Braille --> Dots["다섯 점자칸의 논리값"]
```

문서 해석에서 수식을 만났을 때 구조를 만들고, 읽기용 항목으로 바꿀 때 해석 상태를 붙인다. 그 결과를 음성과 점자 규칙이 소비한다. AST는 이 연결의 공통 표현이고, 해석 상태는 그 표현을 어느 수준까지 출력에 써도 되는지 결정한다. 일부 해석된 수식을 음성으로 안내하면서 점자는 비우는 서로 다른 정책도 같은 입력에 적용할 수 있다.

탐색은 AST의 가지를 직접 이동하지 않는다. 사용자는 페이지·읽기 항목을 고르고, 항목 안 수식 구간과 생성된 점자 창을 이동한다. 따라서 AST가 수식 출력의 근거라는 사실을 ‘분자나 분모를 방향키로 직접 선택하는 기능이 있다’는 뜻으로 확대해서는 안 된다. 수식의 계산·동치 판정이나 원문 인식의 정확성 확인도 이 구조의 역할은 아니다.''')

before_source('4.6','''#### 재전송은 새 촬영과 다르게 식별한다

서버가 파일을 저장한 뒤 응답만 유실되면 단말은 같은 파일을 다시 보낼 수 있다. 요청 식별자와 내용 검사값을 함께 사용하면 서버는 기존 저장 결과를 찾아 수신 확인을 다시 반환할 수 있다. 같은 식별자에 다른 내용을 넣으면 재시도가 아닌 충돌로 처리한다. 새 식별자를 사용해도 서버의 논리적 촬영 작업과 내용이 정확히 같다면 기존 저장을 재사용하는 경로가 있다.

이것은 이미지가 같은 페이지처럼 보이는지를 판단하는 알고리즘이 아니다. 카메라 쪽은 촬영 내용의 중복을, 서버 쪽은 동일 전송 작업의 재실행을 다룬다. 자료 완성 역시 파일 수신과 분리해, 마감한 순번의 페이지·음성 구성이 준비된 뒤 새 버전을 게시한다. 따라서 전송을 여러 번 시도했다는 사실과 교재 페이지가 여러 번 추가됐다는 사실은 같지 않다.''')

before_source('4.8','''#### 읽던 위치의 저장과 지금 출력할 권한을 분리한다

사용자가 다음 항목을 요청했고 서버는 이동을 저장했지만 응답이 유실됐다고 하자. 같은 명령 식별자로 다시 요청하면 저장한 응답을 반환하므로 한 번 더 이동하지 않는다. 새 위치와 명령 처리 기록을 따로 저장했다면 한쪽만 남는 상태를 고려해야 하지만, 현재 경로는 둘을 같은 저장 작업으로 확정한다. 앱 재시작 후 위치 복구도 이 지속 상태를 사용한다.

한편 지속 상태만으로는 늦게 도착한 음성을 막을 수 없다. 이전 항목의 음성 파일을 받는 중 새 항목으로 이동하면, 단말은 이전 작업의 출력 권한을 취소하고 도착 시점에 여전히 유효한 작업인지 확인한다. 서버의 읽기 상태 번호와 단말의 재생 작업 번호를 따로 쓰는 이유가 여기에 있다. 시스템 안내의 우선순위와 취소도 단말 안에서 발생한다.

점자 전송도 최신 요청 상태를 우선하지만, 이전 메시지를 이미 선로에 보냈거나 모터가 움직이는 물리 과정까지 되돌리는 기능은 아니다. 이 방식은 오래된 결과를 구분하고 억제하는 소프트웨어 계약이며, 실제 음성과 핀의 동시 완료 보장은 아니다.''')

insert='''### 검증을 단계별로 나누는 이유

앞 단계의 성공만으로 다음 단계가 필요한 조건을 모두 알 수 없기 때문이다. ‘파일이 정확히 저장됐다’는 ‘문장이 정확히 인식됐다’와 다르고, ‘점자 명령이 맞다’는 ‘핀이 맞게 움직였다’와 다르다. 각 검사는 다음 단계에 넘길 보장을 좁게 정한다.

| 검사 | 넘겨주면 안 되는 실패 | 통과로 확인하는 것 | 통과해도 남는 질문 |
|---|---|---|---|
| 영상 해독·크기 검사 | 깨진 이미지나 과도한 입력 | 처리 가능한 이미지 | 책이 선명하고 방향이 맞는가? |
| 좌우 준비·보정 검사 | 한쪽만 있거나 부적합한 변환 | 전송할 두 이미지 준비 | 본문을 정확히 인식할 수 있는가? |
| 파일 해시·수신 식별자 검사 | 손상 파일·다른 요청의 응답 | 의도한 파일의 저장 확인 | 페이지 해석이 끝났는가? |
| 문서 형식·참조 검사 | 잘못된 필드나 연결 | 읽기 코드가 다룰 구조 | 사진의 내용이 빠짐없이 포함됐는가? |
| 게시 전 페이지·음성 목록 검사 | 누락·변조·미완성 자료 게시 | 읽을 수 있는 버전 구성 | 교재의 의미를 정확히 복원했는가? |
| 음성 파일·작업 권한 검사 | 잘못된 파일·오래된 재생 결과 | 현재 작업에 맞는 음성 자원 | 사용자가 실제로 들었는가? |
| 점자 메시지 형식·범위 검사 | 잘린 메시지·범위 밖 값 | 장치가 처리할 요청 | 제어 보드·모터·핀까지 적용됐는가? |

따라서 모든 검사 결과를 하나의 ‘정상’으로 묶기보다, 실패가 어느 경계를 처음 넘지 못했는지와 이미 확인된 범위를 함께 기록한다. 이 구조가 제공하는 이점은 원인을 나눠 확인하고 잘못된 성공 안내를 줄일 수 있다는 점이다. 모든 원문 오류나 기구 오차를 자동 감지한다는 뜻은 아니다.

'''
s=s.replace('## 7. 대표 실행 사례',insert+'## 7. 대표 실행 사례',1)

examples='''### 같은 작은 페이지가 인식 결과에서 읽기 순서로 바뀌는 과정

다음은 인식 모델의 결과를 통제한 예제다. 실제 사진이나 모델 추론을 실행한 결과는 아니다. 문제표식, ‘다음 분수의 분모를 고르시오.’라는 지문, 분수½, 선택지①1·②2·③3·④4·⑤5가 있는 한 페이지를 사용했다. 파일 목록에서는 수식·선택지·문제표식·지문 순으로 섞어 넣되, 각 영역에 실제 읽기 순서를 따로 지정했다.

| 단계 | 실제로 확인한 변환 |
|---|---|
| 인식 결과 입력 | 영역 네 개가 섞여 있지만 각각 읽기 순서 1~4를 가짐 |
| 페이지 중간 표현 | 문제표식→지문→수식→선택지로 정렬하고 수식에는 분자·분모 구조를 저장 |
| 문제 구성 | 네 항목을 같은 문제에 속하도록 연결 |
| 읽기 목록 | 글·글·수식·글의 네 항목으로 펼침. 문제 묶음 자체를 별도의 중복 발화로 더하지 않음 |
| 중복 참조 추가 | 이미 포함한 같은 항목 식별자를 다시 참조해도 읽기 항목은 네 개 유지 |

비교를 위해 같은 입력에서 수식 영역의 위치 정보만 없앴다. 이 영역은 변환 단계에서 건너뛰었고 읽기 목록은 글 세 개가 됐지만, 남은 문서의 형식 검사는 통과했다. 형식 검사는 데이터 구조를 확인하므로 사진에서 빠진 수식을 증명할 수 없다는 구체적 한계다. 이 결과를 새 실기기 사고의 원인으로 단정하지 않는다.

### 페이지 변경 판단의 세 가지 작은 입력

저장된 기준 번호 쌍이26/27일 때 서로 다른 영상에서 다음 문자열을 관측했다고 가정했다. 실제 카메라가 아니라 관측 수집기에 값을 넣은 예제다.

| 새 관측 | 결과 | 의미 |
|---|---|---|
| 26/27 한 번 | 같음 | 이미 전송한 기준 쌍과 정확 일치 |
| 28/29 다섯 번 | 앞 네 번 판단 불가, 다섯 번째 다름과 번호 변화 근거 | 새 쌍의 반복과 필요한 관측 수 충족 |
| 28/29, 2B/29, 28/2g, 23/29, 28/9 | 다섯 번 뒤에도 판단 불가 | 이전과 모두 달라도 새 내용의 일관성 부족 |

위 결과만으로 실제 촬영 성공을 입증하지는 않는다. 영상 기반 추가 조건, 후보 품질, 보정·전송까지는 별도 단계다.

### 수식 해석이 부분적으로만 성공한 경우

| 통제한 문자열 입력 | 상태와 음성용 문장 | 점자값 |
|---|---|---|
| 분수½의 정상 표기 | 해석 완료, ‘2분의 1’ | 다섯 칸 생성 |
| x 더하기 미지원 명령 | 일부 해석, 불확실 안내와 해석 가능한 부분 | 비움 |
| 분수 명령 뒤 괄호가 끝나지 않은 표기 | 해석 실패, ‘수식 인식이 불확실합니다.’ | 비움 |

세 예제는 실제 함수·페이지 처리 경로에 통제 입력을 넣은 결과다. 정확한 문자열·중간 표현·상태·점자값은 [원시 예제 결과](technical-overview/evidence/core-principles-examples.json), 호출 범위와 source/test 근거는 [근거 카드](technical-overview/CORE_PRINCIPLES_EVIDENCE_CARDS.md)에 보존했다. 정상 결과만 보여 주지 않고 판단 불가와 불완전한 해석도 같은 입력 흐름 안에서 비교한다.

'''
s=s.replace('## 8. 실패 처리와 현재 한계',examples+'## 8. 실패 처리와 현재 한계',1)
target.write_text(s,encoding='utf-8')
print('Added core explanations and examples; existing Korean paragraphs retained.')
